# Guardian - Offline LLM Agent
# Optimized for 8GB RAM systems

# LLM Backend
llama-cpp-python>=0.2.50  # Necessario para inferencia local

# Web Search (Versao relaxada para evitar conflito com click)
duckduckgo-search
requests==2.27.1
beautifulsoup4
watchdog==2.3.1
click>=8.0.4

# UI Framework (Pure Python - sem dependencias de sistema)
customtkinter>=5.2.0

# Utilities
pyyaml>=5.3,<6.0
rich==12.6.0
psutil>=5.6
